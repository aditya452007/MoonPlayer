import { PageTransition } from '../../components/layout/PageTransition/PageTransition';

export function Settings() {
  return (
    <PageTransition>
      <div>
        <h1>Settings</h1>
        <p>User preferences, Data Saver, Pet toggles will go here.</p>
      </div>
    </PageTransition>
  );
}
